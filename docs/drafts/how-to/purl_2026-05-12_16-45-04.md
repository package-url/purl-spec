
# How to Parse


File info:

- `TEST ID`: 01

- `INPUT PURL`: "pkg:alpm/arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123#sub/path%2B1%402%3A3"

- Test script: `src/2026-parse-purl/test-base-and-advanced-structure-2026.05.12.v02.py`


- Date: 2026-05-12 16:45 Tue

---

This guide assumes that readers are familiar with:

- the ECMA-427 specification (https://ecma-tc54.github.io/ECMA-427/#sec-purl-specification) and

- the terms of the relevant `[type]-definition.json` file (https://github.com/package-url/purl-spec/tree/main/types) (collectively the `PURL SPEC`).

The parsing process described below covers both the "base" and "advanced" test groups. While the current version of the `PURL SPEC` defines mandatory ("shall") as well as permitted ("should") normalization steps, the "base" group is defined to exclude all such normalization and to require strict compliance with the definition of a canonical PURL.

- "base": A parser is expected to raise an error if a "base" test case input string is not a canonical PURL.

- "advanced": This test group is designed to support flexible PURL building and parsing.

We'll use this `INPUT PURL` to illustrate the steps.
- `INPUT PURL`: "pkg:alpm/arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123#sub/path%2B1%402%3A3"

---

## 1. Scheme ("base")

- Split the input string once from the far left on ':'.

- The left side is the **scheme**.

  - **scheme** = "pkg"

- The right side is the `SCHEME REMAINDER`.

  - `SCHEME REMAINDER` = "alpm/arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123#sub/path%2B1%402%3A3"

- Do not strip any leading slashes '/' (e.g., '/', '//', '///' and so on) from the `SCHEME REMAINDER`.

  - `SCHEME REMAINDER` = "alpm/arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123#sub/path%2B1%402%3A3"

---------------------------------------------------------

## 1. Scheme ("advanced")

- Split the input string once from the far left on ':'.

- The left side is the **scheme**.

  - **scheme** = "pkg"

- The right side is the `SCHEME REMAINDER`.

  - `SCHEME REMAINDER` = "alpm/arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123#sub/path%2B1%402%3A3"

- Strip all leading slashes '/' (e.g., '/', '//', '///' and so on) from the `SCHEME REMAINDER`.

  - `SCHEME REMAINDER` = "alpm/arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123#sub/path%2B1%402%3A3"

---------------------------------------------------------

## 2. Type ("base")

- Split the `SCHEME REMAINDER` once from the left on '/'.

- The left side is the **type**.

  - **type** = "alpm"

- The right side is the `TYPE REMAINDER`.

  - `TYPE REMAINDER` = "arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123#sub/path%2B1%402%3A3"

---------------------------------------------------------

## 2. Type ("advanced")

- Split the `SCHEME REMAINDER` once from the left on '/'.

- The left side is the **type**.

  - **type** = "alpm"

- The right side is the `TYPE REMAINDER`.

  - `TYPE REMAINDER` = "arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123#sub/path%2B1%402%3A3"

---------------------------------------------------------

## 3. Namespace ("base")

- In this **namespace** section, we'll begin by collecting some data from the far right of the input string -- for the **subpath** and **qualifiers** components (if any).

### A. Split 'TYPE REMAINDER' ("base")

- Split the `TYPE REMAINDER` once from the right on '#'.

- The left side is the `NAMESPACE-QUALIFIERS SEGMENT`

  - `NAMESPACE-QUALIFIERS SEGMENT` = "arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123"

- The right side is the `SUBPATH STRING`

  - `SUBPATH STRING` = "sub/path%2B1%402%3A3"

- We'll return to the `SUBPATH STRING` in section 7 (Subpath) below.

### B. Split 'NAMESPACE-TO-QUALIFIERS SEGMENT' ("base")

- Split the `NAMESPACE-TO-QUALIFIERS SEGMENT` once from the right on '?'.

- The left side is the `NAMESPACE-TO-VERSION SEGMENT`

  - `NAMESPACE-TO-VERSION SEGMENT` = "arch/containers-common@1:0.47.4-4"

- The right side is the `QUALIFIERS STRING`

  - `QUALIFIERS STRING` = "arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123"

- We'll return to the `QUALIFIERS STRING` in section 6 (Qualifiers) below.

### C. Split 'NAMESPACE-TO-VERSION SEGMENT' ("base")

- Split the `NAMESPACE-TO-VERSION SEGMENT` once from the right on '/'.

- The left side is the `NAMESPACE STRING`

  - `NAMESPACE STRING` = "arch"

- The right side is the `NAME-TO-VERSION SEGMENT`

  - `NAME-TO-VERSION SEGMENT` = "containers-common@1:0.47.4-4"

- If the `NAMESPACE STRING` has no value, there is no **namespace**.

- If the `NAMESPACE STRING` has a value:
  - Do not remove any leading or trailing slashes '/'.
  - Split the `NAMESPACE STRING` on '/'.
  - Percent-decode each segment.
  - UTF-8-decode each segment if needed in your programming language.
  - Join the segments back with a '/'.

- This is the **namespace**:

  - **namespace** = "arch"

---------------------------------------------------------

## 3. Namespace ("advanced")

- In this **namespace** section, we'll begin by collecting some data from the far right of the input string -- for the **subpath** and **qualifiers** components (if any).

### A. Split 'TYPE REMAINDER' ("advanced")

- Split the `TYPE REMAINDER` once from the right on '#'.

- The left side is the `NAMESPACE-QUALIFIERS SEGMENT`

  - `NAMESPACE-QUALIFIERS SEGMENT` = "arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123"

- The right side is the `SUBPATH STRING`

  - `SUBPATH STRING` = "sub/path%2B1%402%3A3"

- We'll return to the `SUBPATH STRING` in section 7 (Subpath) below.

### B. Split 'NAMESPACE-TO-QUALIFIERS SEGMENT' ("advanced")

- Split the `NAMESPACE-TO-QUALIFIERS SEGMENT` once from the right on '?'.

- The left side is the `NAMESPACE-TO-VERSION SEGMENT`

  - `NAMESPACE-TO-VERSION SEGMENT` = "arch/containers-common@1:0.47.4-4"

- The right side is the `QUALIFIERS STRING`

  - `QUALIFIERS STRING` = "arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123"

- We'll return to the `QUALIFIERS STRING` in section 6 (Qualifiers) below.

### C. Split 'NAMESPACE-TO-VERSION SEGMENT' ("advanced")

- Split the `NAMESPACE-TO-VERSION SEGMENT` once from the right on '/'.

- The left side is the `NAMESPACE STRING`

  - `NAMESPACE STRING` = "arch"

- The right side is the `NAME-TO-VERSION SEGMENT`

  - `NAME-TO-VERSION SEGMENT` = "containers-common@1:0.47.4-4"

- If the `NAMESPACE STRING` has no value, there is no **namespace**.

- If the `NAMESPACE STRING` has a value:
  - Remove all leading and trailing slashes '/'.
  - Split the `NAMESPACE STRING` on '/'.
  - Discard any empty segment from that split.
  - Percent-decode each segment.
  - UTF-8-decode each segment if needed in your programming language.
  - Join the segments back with a '/'.

- This is the **namespace**:

  - **namespace** = "arch"

---------------------------------------------------------

## 4. Name ("base")

- Split the `NAME-VERSION SEGMENT` once from the right on '@'.

- The left side is the `NAME STRING`.

  - `NAME STRING` = "containers-common"

- The right side is the `VERSION STRING`.

  - `VERSION STRING` = "1:0.47.4-4"

- If the `NAME STRING` has a value:

  - Do not remove any leading or trailing slashes '/' from the `NAME STRING`.
  - Percent-decode the `NAME STRING`.
  - UTF-8-decode the `NAME STRING` if needed in your programming language.

- This is the **name**:

  - **name** = "containers-common"

---------------------------------------------------------

## 4. Name ("advanced")

- Split the `NAME-VERSION SEGMENT` once from the right on '@'.

- The left side is the `NAME STRING`.

  - `NAME STRING` = "containers-common"

- The right side is the `VERSION STRING`.

  - `VERSION STRING` = "1:0.47.4-4"

- If the `NAME STRING` has a value:

  - Remove all leading and trailing slashes '/' from the `NAME STRING`.
  - Percent-decode the `NAME STRING`.
  - UTF-8-decode the `NAME STRING` if needed in your programming language.

- This is the **name**:

  - **name** = "containers-common"

---------------------------------------------------------

## 5. Version ("base")

- In the previous section, we defined `VERSION STRING` as "1:0.47.4-4".

- Percent-decode the `VERSION STRING`.
- UTF-8-decode the `VERSION STRING` if needed in your programming language.

- This is the **version**:

  - **version** = "1:0.47.4-4"

---------------------------------------------------------

## 5. Version ("advanced")

- In the previous section, we defined `VERSION STRING` as "1:0.47.4-4".

- Percent-decode the `VERSION STRING`.
- UTF-8-decode the `VERSION STRING` if needed in your programming language.

- This is the **version**:

  - **version** = "1:0.47.4-4"

---------------------------------------------------------

## 6. Qualifiers ("base")

- In section 3.B, we defined the `QUALIFIERS STRING`:

  - `QUALIFIERS STRING` = "?"

- Split the `QUALIFIERS STRING` on '&'. Each part is a 'key=value' pair.

- For each pair, split the 'key=value' once from the left on '='.

  - The 'key' is the left side.

  - Percent-decode the right side -- this is the 'value'.

  - UTF-8-decode the 'value- if needed in your programming language.

- This is the **qualifiers** object:

- **qualifiers** = 


```
{
  "arch": "x86_64",
  "build_type": "Debug",
  "compiler": "Visual Studio:123"
}
```

---------------------------------------------------------

## 6. Qualifiers ("advanced")

- In section 3.B, we defined the `QUALIFIERS STRING`:

  - `QUALIFIERS STRING` = "?"

- Split the `QUALIFIERS STRING` on '&'. Each part is a 'key=value' pair.

- For each pair, split the 'key=value' once from the left on '='.

  - The 'key' is the left side.

  - Percent-decode the right side -- this is the 'value'.

  - UTF-8-decode the 'value- if needed in your programming language.

- This is the **qualifiers** object:

- **qualifiers** = 


```
{
  "arch": "x86_64",
  "build_type": "Debug",
  "compiler": "Visual Studio:123"
}
```

---------------------------------------------------------

## 7. Subpath ("base")

- We defined the `SUBPATH STRING` in section 3.A:

  - `SUBPATH STRING` = "sub/path%2B1%402%3A3"

- If the `SUBPATH STRING` has a value:
  - Do not remove any leading or trailing slashes '/'.
  - Split the `SUBPATH STRING` on '/'.
  - Percent-decode each segment.
  - UTF-8-decode each segment if needed in your programming language.
  - Join the segments back with a '/'.

- This is the **subpath**:

  - **subpath** = "sub/path+1@2:3"

## 7. Subpath ("advanced")

- We defined the `SUBPATH STRING` in section 3.A:

  - `SUBPATH STRING` = "sub/path%2B1%402%3A3"

- If the `SUBPATH STRING` has a value:
  - Remove all leading and trailing slashes '/'.
  - Split the `SUBPATH STRING` on '/'.
  - Discard any empty segment from that split.
  - Percent-decode each segment.
  - UTF-8-decode each segment if needed in your programming language.
  - Discard any '.' or '..' segment from that split.
  - Join the segments back with a '/'.

- This is the **subpath**:

  - **subpath** = "sub/path+1@2:3"

<div style="margin-top: 50px;" /div>

---
## Summary
`INPUT PURL`: "pkg:alpm/arch/containers-common@1:0.47.4-4?arch=x86_64&build_type=Debug&compiler=Visual%20Studio:123#sub/path%2B1%402%3A3"

Parsed `INPUT PURL`:
<table>
<th>Component</th>
<th>"base"</th>
<th>"advanced"</th>
<tr>
<td>
<b>scheme</b>
</td>
<td>
"pkg"
</td>
<td>
"pkg"
</td>
</tr>
<tr>
<td>
<b>type</b>
</td>
<td>
"alpm"
</td>
<td>
"alpm"
</td>
</tr>
<tr>
<td>
<b>namespace</b>
</td>
<td>
"arch"
</td>
<td>
"arch"
</td>
</tr>
<tr>
<td>
<b>name</b>
</td>
<td>
"containers-common"
</td>
<td>
"containers-common"
</td>
</tr>
<tr>
<td>
<b>version</b>
</td>
<td>
"1:0.47.4-4"
</td>
<td>
"1:0.47.4-4"
</td>
</tr>
<tr>
<td>
<b>qualifiers</b>
</td>
<td>

```
{
  "arch": "x86_64",
  "build_type": "Debug",
  "compiler": "Visual Studio:123"
}
```
</td>
<td>

```
{
  "arch": "x86_64",
  "build_type": "Debug",
  "compiler": "Visual Studio:123"
}
```
</td>
</tr>
<tr>
<td>
<b>subpath</b>
</td>
<td>
"sub/path+1@2:3"
</td>
<td>
"sub/path+1@2:3"
</td>
</tr>
</table>
