{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://purl-spec.org/schemas/purl-type-index.schema-1.0.json",
  "title": "Package-URL Type Index",
  "description": "An index of all known PURL types and their definitions.",
  "type": "object",
  "properties": {
    "last_updated": {
      "type": "string",
      "format": "date-time",
      "description": "The timestamp when the index was last updated, in ISO 8601 format."
    },
    "types": {
      "type": "array",
      "description": "A list of known PURL types.",
      "additionalItems": false,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "type": { "type": "string", "description": "The name of the PURL type (e.g., 'maven', 'npm', 'pypi')." },
          "definition_url": { "type": "string", "format": "uri", "description": "URL pointing to the PURL Type Definition JSON Schema." }
        },
        "required": ["name", "schema_url"]
      }
    }
  }
}
